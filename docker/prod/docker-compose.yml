version: '3'
services:
  web:
    image: teste
    environment:
      - MONGODB=mongodb://mongo:27017/express-app
      - PORT=8000
    ports:
      - '8000:8000'
    links:
      - mongo
  mongo:
    container_name: mongo
    image: mongo
    ports:
      - '27017:27017'